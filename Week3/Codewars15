//помог чат гпт, но не совсем понял.

function parse(data) {              
  let res = [];                     //сделал пустой массив для хранение результата
  data.split('').reduce((cur, s) => {   //прохожу по каждому элементу строки
    if (s === 'i') cur++;           //если i, то увеличиваем на 1
    if (s === 'd') cur--;           //если d, то уменьшаем на 1
    if (s === 's') cur = Math.pow(cur, 2);  //если s, возводим в степень
    if (s === 'o') res.push(cur);   //если o, добавляем текущее число в массив
    
    return cur;
  }, 0);            //не до конца понял 
  
  return res;
}