//Задача
//В этой простой задаче вам нужно создать функцию, которая преобразует строку в мексиканскую волну. Вам передаётся строка, а вы должны вернуть массив строк, где заглавная буква — это стоящий человек.

//Правила
//1. Входная строка всегда будет состоять из строчных букв и пробелов, но может быть пустой. В этом случае необходимо вернуть пустой массив. 2. Если символ в строке — пробел, то пропустить его, как если бы это было пустое место.

//Примеры
//"hello" => ["Hello", "hEllo", "heLlo", "helLo", "hellO"]



function wave(str){
  let strwave = [];       //сделал пустой массив
for (let i = 0; i < str.length; i++) {  //цикл
    let words = str.split('');       //разделил строки в массиве
    if(words[i] !== ' ') {        //избавился от пробелов
  words[i] = words[i].toUpperCase()  //Делаю заглавные буквы в строке с помощью "toUpperCase"
    strwave.push(words.join(''))  //объединяю массив символов и добавляю в пустой массив, который создал
  }
  }
  return strwave;     //возвращаю готовой массив символов
}