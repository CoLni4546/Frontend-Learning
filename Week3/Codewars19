//Средиземье вот-вот разразится война. Силам добра предстоит немало битв с силами зла. В них, безусловно, будут участвовать разные расы. У каждой расы есть свои особенности worthв сражениях с другими. На стороне добра представлены следующие расы и их представители worth:

//Хоббиты: 1, Мужчин: 2, Эльфы: 3, Гномы: 3, Орлы: 4, Волшебники: 10
//На стороне зла у нас:

//Орки: 1, Мужчин: 2, Варги: 2, Гоблины: 2, Урук Хай: 3, Тролли: 5, Волшебники: 10
//Хотя погода, местоположение, припасы и доблесть играют роль в любом сражении, если сложить достоинства стороны добра и сравнить их с достоинством стороны зла, то победит, как правило, та сторона, у которой больше достоинств.

//Таким образом, учитывая количество каждой расы на стороне добра, а затем количество каждой расы на стороне зла, определите, какая сторона победит.

//Вход:
//Функция будет принимать два параметра. Каждый параметр будет представлять собой строку из нескольких целых чисел, разделённых одним пробелом. Каждая строка будет содержать количество представителей каждой расы на стороне добра и зла.

//Первый параметр будет содержать количество каждой расы на стороне добра в следующем порядке:

//Хоббиты, люди, эльфы, гномы, орлы, волшебники.
//Второй параметр будет содержать количество каждой расы на стороне зла в следующем порядке:

//Орки, люди, варги, гоблины, уруки, тролли, волшебники.
//Все значения — неотрицательные целые числа. Итоговая сумма значений каждой стороны не превысит предел 32-битного целого числа.

//Выход:
//Вернитесь "Battle Result: Good triumphs over Evil", если победит добро, "Battle Result: Evil eradicates all trace of Good"если победит зло или "Battle Result: No victor on this battle field"если все закончится ничьей.


function goodVsEvil(good, evil){
   let goodForces = good.split(" ");    //вставил пробелы в строку
   let evilForces = evil.split(" ");    //вставил пробелы в строку
   
   let goodArmy = new Map([                                 //создал армию добра(посчитал силу каждого персонажа)
                           ["Hobbits", goodForces[0] * 1 ],
                           ["Men"    , goodForces[1] * 2 ],
                           ["Elves"  , goodForces[2] * 3 ],
                           ["Dwarves", goodForces[3] * 3 ],
                           ["Eagles" , goodForces[4] * 4 ],
                           ["Wizards", goodForces[5] * 10]
                         ]);    
   
   
   let evilArmy = new Map([                                  //создал армию зла(посчитал силу каждого персонажа)
                           ["Orcs"    , evilForces[0] * 1 ],
                           ["Men"     , evilForces[1] * 2 ],
                           ["Wargs"   , evilForces[2] * 2 ],
                           ["Goblins" , evilForces[3] * 2 ],
                           ["Uruk Hai", evilForces[4] * 3 ],
                           ["Trolls"  , evilForces[5] * 5 ],
                           ["Wizards" , evilForces[6] * 10]
                         ]);
   
   
   let goodres = 0;     //создал переменную с результатом подсчета армии добра
   for(let value of goodArmy.values()) goodres += value;   //суммирую через цикл силы сторон добра
   
   
   let evilres = 0;     //создал переменную с результатом подсчета армии зла
   for(let value of evilArmy.values()) evilres += value;   //суммирую через цикл силы сторон зла
   
   if (goodres == evilres){
    return "Battle Result: No victor on this battle field"};
   if (goodres > evilres){
    return "Battle Result: Good triumphs over Evil"};
   if (goodres < evilres){
    return "Battle Result: Evil eradicates all trace of Good"};     //возвратил значения через "if"
}