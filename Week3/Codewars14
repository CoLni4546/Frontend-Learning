//Вы отправились в поход с друзьями вдали от дома, но когда пришло время возвращаться, вы обнаружили, что бензин на исходе,
//  а ближайшая заправка находится 50в нескольких милях отсюда! Вы знаете, что в среднем ваша машина проезжает около 25одного галлона топлива. 
// Осталось ещё 2несколько галлонов.
//Учитывая эти факторы, напишите функцию, которая сообщит вам, возможно ли добраться до колонки или нет.
//Функция должна возвращать значение, trueесли это возможно, и falseесли нет.


const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
  var fuel = mpg * fuelLeft      //умножил сколько проезжает машина в среднем и кол-во галлонов
  if(fuel >= distanceToPump){    //если "fuel" больше получилось чем дистанция до заправки, то возращаем "true"
    return true;    
  }
  else {
    return false;
  }
};