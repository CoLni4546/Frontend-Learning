#git push - команда, которая отпраляет локальные комиты на удаленный сервер
#git pull - команда, которая забирает последние изменения с удаленного сервера и объединяет их с вашей локальной копией проекта.
#git status - команда, которая показывает текущее состояние вашего рабочего каталога и индекса (staging area).
             #Она отвечает на вопросы: "Что сейчас происходит в моем репозитории?" и "Какие файлы были изменены?"
#git add - команда, которая добавляет изменения из ваших рабочих файлов в "промежуточную область" (staging area), подготавливая их к коммиту.
#git commit - команда, которая сохраняет ваши подготовленные изменения (добавленные через git add) в историю проекта как новый "снимок" состояния файлов.
#git config --global esur.name/email - команды для настройки вашего имени и email-адреса в Git
#git branch -M main - команда, которая переименовывает текущую ветку в main
#node "index.js"- команда для запуска JavaScript-файла в среде выполнения Node.js.
#git remote add origin - 