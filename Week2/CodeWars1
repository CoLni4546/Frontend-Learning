#Таракан — одно из самых быстрых насекомых. Напишите функцию, которая принимает его скорость в км/ч и возвращает её в см/с, округлённую до целого числа (= floored).

#Например:

#1.08 --> 30
#Примечание! Входные данные — вещественное число (фактический тип зависит от языка) >= 0. Результат должен быть целым числом.


function cockroachSpeed(s) { 

$ в следующий раз именуй переменную осознанно $
  let b = Math.floor(s * 100000 / 3600); #в инете нашел как перевести км/ч в см/сек(сделал переменную "b"-там все хранится
  console.log(b);(вывел число)
  return b; (так-же почитал и узнал про Math.floor)
  
  
}
