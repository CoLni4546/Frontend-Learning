#Бобу нужен быстрый способ вычисления объема прямоугольного параллелепипеда с использованием трех величин: length, widthи heightпараллелепипеда.

#Напишите функцию, которая поможет Бобу с этим вычислением.


#Ну это было самое сложное!!!

class Kata {
  static getVolumeOfCuboid(length, width, height) {
    return length * width * height; #возвратил по формуле прямоугольного параллелепипеда его объем (a*b*c)
  }
}