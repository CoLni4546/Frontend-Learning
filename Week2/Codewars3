$ в этой задаче хромают отступы, везде разные, код выглядит кривым, комментарии делай на одном уровне. Перед коммитом прогоняй линтер! $


#Дезоксирибонуклеиновая кислота, ДНК, является основной молекулой хранения информации в биологических системах. Она состоит из четырёх оснований нуклеиновых кислот: гуанина (G), цитозина (C), аденина (A) и тимина (T).
#Рибонуклеиновая кислота, РНК, является основной молекулой-мессенджером в клетках. РНК несколько отличается от ДНК по химической структуре и не содержит тимина. В РНК тимин заменяется другой нуклеиновой кислотой – урацилом (U).

#Создайте функцию, которая переводит заданную строку ДНК в РНК.

#Например:

#"GCAT"  =>  "GCAU"
#Входная строка может быть произвольной длины, в частности, она может быть пустой. Все входные данные гарантированно корректны, т.е. каждая входная строка всегда будет состоять только из 'G', 'C'и 'A'/или 'T'.

$ вот тут например $
     # Чуть помог чат gpt, он помог по минимуму, эту задачку когда-то решал
function DNAtoRNA(dna) {
  let dnatorna = ''; #Создал переменную для сохранения результата
  $ тут тоже for уехал на пробел куда-то $
   for (let i = 0; i < dna.length; i++) {  #Использую цикл чтобы пересчитать каждый элемент и заменить нужные 
    let Dna = dna[i];  #Определил 1 элемент из всего массива
    
     if (Dna === 'T') {  #Если элемент = T, то вместо этого символа ставится U.  Тут помог chat gpt(изменил "==" на "===")
      dnatorna += 'U'; # Прировнял к T и добавил с троку
    }
     else {
      dnatorna += Dna;  # Если символы не "T", то оставляем их в покое и вставляем в строй
    }
  }
     return dnatorna;  #Возвращаем всё
     }
    
